type User @entity {
  id: ID!
  address: String!
  balance: BigInt!
  updatedAt: BigInt!
  transfers: [Transfer!]! @derivedFrom(field: "from")
}

type Transfer @entity(immutable: true) {
  id: ID!
  txHash: String!
  from: User!
  to: User!
  value: BigInt!
  timestamp: BigInt!
}